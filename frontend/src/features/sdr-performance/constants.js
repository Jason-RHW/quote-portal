// Chart palette — matches sdr-daily-report/chart_data.py exactly, so the
// colors here are identical to the ones in the daily email PDF.
export const ACCENTS = ["var(--sdr-1)", "var(--sdr-2)", "var(--sdr-3)", "var(--sdr-4)", "var(--sdr-5)"];
